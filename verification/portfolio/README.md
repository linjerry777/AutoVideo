# AutoVideo portfolio evidence

These screenshots show the local operator dashboard with non-sensitive runtime
data. They contain no credentials, customer records, or generated media files.

- `autovideo-dashboard-desktop.png`: desktop control-center view.
- `autovideo-dashboard-mobile.png`: responsive mobile control-center view.

The app itself remains local-only and is served from `http://127.0.0.1:9000/ui`.
